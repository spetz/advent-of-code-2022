use std::{fs, io::BufReader};
use std::io::BufRead;
use regex::Regex;

const PATH: &str = "./src/input.txt";

fn main() {
    let result = part_1();
    println!("{}", result);
}

fn part_1() -> usize {
    // TODO
    0
}